#!/usr/bin/python3

from setuptools import setup, find_packages

setup(
    name='KRunner-Bridge',
    version='1.0.0',
    description='Utility for the KRunner-Bridge plugin',
    url='https://github.com/alex1701c/krunner-bridge',
    entry_points={},
    packages=find_packages(),
)
